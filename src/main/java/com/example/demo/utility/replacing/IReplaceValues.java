package com.example.demo.utility.replacing;

import java.util.Map;

public interface IReplaceValues {
    String replaceValues(String itemToReplace, String replacement, String text);

    String dinamicMapValueGrant(String email, String v1);
}
